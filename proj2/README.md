## proj2
This bash script sanitizes logs from exposing unwanted information and reprints the filtered version into another log file with a date and timestamp. It also checks if the log its filtering has outdated.
